{
  "name": "enable-github",
  "version": "0.1.0",
  "description": "GitHub Action to enable Homebrew in Ubuntu virtual environments",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^2.0.3"
  }
}
